<h2>Connexion</h2>

<p>Utilisez votre email prenom.nom@mmibordeaux.com pour vous connecter. Pour votre première connexion, cliquez sur "Mot de passe oublié", entrez votre email prenom.nom@mmibordeaux.com, cliquez sur le lien dans l'email reçu et définissez votre mot de passe.</p>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, required: false, autofocus: true %>
    <%= f.input :password, required: false %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Connexion" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
