<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Learn" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= favicon_link_tag 'favicon.png' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar navbar-static-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to root_path, class:'navbar-brand logo logo-black' do %>
            <%= image_tag 'mmi-bordeaux.png' %>
            <p>Learn</p>
          <% end %>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav pull-right">
            <% if student_signed_in? %>
              <li><%= link_to 'Accueil', root_path %></li>
              <li><%= link_to 'Mon profil', me_path %></li>
              <% if current_student.admin %>
                <li class="dropdown">
                  <a data-toggle="dropdown">Admin <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to 'Cours', admin_courses_path %></li>
                    <li><%= link_to 'Réalisations', admin_achievements_path %></li>
                    <li><%= link_to 'Evénements', admin_events_path %></li>
                    <li><%= link_to 'Promotions', admin_promotions_path %></li>
                    <li><%= link_to 'Etudiants', admin_students_path %></li>
                  </ul>
                </li>
              <% end %>
              <li><%= link_to 'Déconnexion', destroy_student_session_path, method: :delete %></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
    <div class="container">
      <% unless notice.nil? %>
        <p class="alert alert-success">
          <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
          <%= notice %>
        </p>
      <% end %>
      <% unless alert.nil? %>
        <p class="alert alert-warning">
          <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
          <%= alert %>
        </p>
      <% end %>
      <div>
        <%= yield %>
      </div>
      <footer>
        <p class="pull-right"><small>
            Ce projet est open source, le code est disponible sur <a href="https://github.com/arnaudlevy/mmibordeaux-learn">Github</a>
        </small></p>
        <p><small><a href="https://www.mmibordeaux.com/">MMI Bordeaux</a></small></p>
      </footer>
    </div><!-- /.container -->
  </body>
</html>