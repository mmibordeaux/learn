<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "My Students" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="navbar navbar-static-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>">My Students</a>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav pull-right">
            <% if student_signed_in? %>
              <li><%= link_to 'Accueil', root_path %></li>
              <li>
                <%= link_to('Logout', destroy_student_session_path, :method => :delete) %>      
              </li>
              <li>
                <%= link_to 'Promotions', promotions_path %>
              </li>
              <li>
                <%= link_to 'Etudiants', students_path %>
              </li>
              <% if current_student.admin %>
                <li><%= link_to 'Réalisations', achievements_path %></li>
              <% end %>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
    <div class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <div>
        <%= yield %>
      </div>
    </div><!-- /.container -->
    <footer></footer>
    <%= javascript_include_tag "application" %>
  </body>
</html>